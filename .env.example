# -----------------------------------------------------------------------------
# DATABASE - Required for all environments
# -----------------------------------------------------------------------------
# Connection string for your PostgreSQL database.
# Local dev (Docker): postgresql://user:password@localhost:5432/clickvid
# Production: Your actual production database connection string.
DATABASE_URL="postgresql://user:password@localhost:5432/clickvid"

# -----------------------------------------------------------------------------
# AUTHENTICATION (Auth.js) - Required
# -----------------------------------------------------------------------------
# The canonical URL of your production site.
# Local dev: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# A strong, random secret used to encrypt JWTs and cookies.
# Generate one with: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-super-strong-nextauth-secret"

# Google OAuth credentials from your Google Cloud Console project.
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Comma-separated list of emails to be automatically promoted to SUPER_ADMIN role on first sign-in.
ADMIN_EMAILS="you@yourdomain.com,another.admin@example.com"

# -----------------------------------------------------------------------------
# S3-COMPATIBLE STORAGE - Required for file uploads & AI generation
# -----------------------------------------------------------------------------
# The full endpoint URL for your S3 provider.
# Example for Wasabi US East 1: https://s3.us-east-1.wasabisys.com
S3_ENDPOINT=""

# The region of your S3 bucket.
S3_REGION="us-east-1"

# The name of your S3 bucket.
S3_BUCKET="clickvid-assets"

# Your S3 provider Access Key ID.
S3_ACCESS_KEY_ID=""

# Your S3 provider Secret Access Key.
S3_SECRET_ACCESS_KEY=""

# -----------------------------------------------------------------------------
# BACKGROUND JOBS (BullMQ) - Optional for dev, Required for prod scaling
# -----------------------------------------------------------------------------
# Connection string for your Redis instance.
# Local dev (Docker): redis://localhost:6379
REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# EMAIL (SMTP) - Required for notifications
# -----------------------------------------------------------------------------
# The "From" address for system emails.
MAIL_FROM="ClickVid Pro <noreply@your-domain.com>"

# Local dev (Mailpit): mailhog
# Production: Your SMTP provider's hostname (e.g., smtp.postmarkapp.com)
SMTP_HOST="mailpit"

# Local dev (Mailpit): 1025
# Production: e.g., 587
SMTP_PORT="1025"

# Production: Your SMTP username
SMTP_USER=""

# Production: Your SMTP password or app-specific token
SMTP_PASS=""

# -----------------------------------------------------------------------------
# AI SERVICES (Genkit) - Required for AI features
# -----------------------------------------------------------------------------
# Add API keys for any AI services you intend to use.
# The system will use the first available key from a priority list.
GEMINI_API_KEY=""
# OPENAI_API_KEY=""
# ELEVENLABS_API_KEY=""
# PEXELS_API_KEY=""
# ...etc.

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT - Required
# -----------------------------------------------------------------------------
# Set to "production" for production builds, "development" otherwise.
NODE_ENV="development"
